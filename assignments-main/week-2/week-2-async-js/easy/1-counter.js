
let counter = 1;
function callback() {
    console.clear(); // it is a clear console function that help in cleaning the console;
    console.log(counter);
    counter++;
}
setInterval(callback, 10000);


/*
SetTimout and SetInterval are two functions which can be used to create repetitive tasks

like running a code every second or running a code every 10 seconds etc;

 */